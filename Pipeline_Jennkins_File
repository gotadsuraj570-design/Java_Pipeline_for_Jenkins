pipeline {

    agent any // Runs on any available agent
    stages {
          stage('clone') {
                      steps {
                           git branch: 'main', url: 'https://github.com/gotadsuraj570-design/Java_Pipeline_for_Jenkins.git'
                              }
                         }

          stage('Compile') {
                     steps {
                          bat 'java Hello_World.java'
                           }

                         }

          stage('Run') {

                      steps {
                         bat 'java Hello_World'
                            }

                         }

           }
      }
